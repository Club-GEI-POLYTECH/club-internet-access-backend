# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_DATABASE=internet_access

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# MikroTik Configuration
MIKROTIK_HOST=192.168.88.1
MIKROTIK_PORT=8728
MIKROTIK_USER=admin
MIKROTIK_PASSWORD=your-mikrotik-password

# Application Configuration
PORT=4000
NODE_ENV=development
# URL du frontend pour CORS (séparer plusieurs URLs par des virgules)
# Exemple: FRONTEND_URL=http://localhost:3000,https://votre-frontend.railway.app
FRONTEND_URL=http://localhost:3000
APP_NAME=Club Internet Access UNIKIN

# SMTP Configuration (MailHog pour développement)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@unikin.cd

# Kafka Configuration
# Activer/désactiver Kafka (true/false)
KAFKA_ENABLED=true
# Brokers Kafka (séparer plusieurs brokers par des virgules)
# Exemple: KAFKA_BROKERS=localhost:9092,localhost:9093
KAFKA_BROKERS=localhost:9092
# Client ID pour Kafka
KAFKA_CLIENT_ID=internet-access-backend
# Authentification Kafka (optionnel)
KAFKA_USERNAME=
KAFKA_PASSWORD=
# Autoriser la création automatique de topics (true/false)
KAFKA_AUTO_TOPIC_CREATION=false

# Ticket Encryption Key (pour chiffrer les mots de passe des tickets)
# Changez cette clé en production et gardez-la secrète
TICKET_ENCRYPTION_KEY=default-encryption-key-change-in-production-32chars!!

